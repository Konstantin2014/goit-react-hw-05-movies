import { Navigation } from '../Navigation//Navigation';
import { Container } from './Layout.styled';

export const AppBar = () => {
  return (
    <Container>
      <Navigation />
    </Container>
  );
};
